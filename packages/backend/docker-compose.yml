services:
  opf:
    image: ghcr.io/your-org/pii-remover-backend:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pii-remover-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      OPF_DEVICE: cpu
      OPF_HOST: 0.0.0.0
      OPF_PORT: "8000"
      OPF_LOG_LEVEL: info
    healthcheck:
      test: ["CMD", "curl", "-fs", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 120s
    volumes:
      - opf_hf_cache:/home/app/.cache/huggingface

volumes:
  opf_hf_cache:
